# F-006-U-6 — role-prompt + CLAUDE.md updates

## PR
#50 · https://github.com/boctorj/agent-orchestrator/pull/50
Status: in_ci (2026-05-21T14:43:34.301815+00:00 UTC)
PR head SHA: 08eb80eadcb6b01695fe850237e7701ffd1605fe

## Coder's PR description (verbatim, as of last capture)
**Unit ID:** F-006-U-6

## What this change does

Final Phase 1 unit of the per-feature spec + cycle log + role prompt rollout
(see [`docs/PROPOSAL-feature-spec-and-headless-daemon.md`](docs/PROPOSAL-feature-spec-and-headless-daemon.md)
§ "Role prompt changes" + § "CLAUDE.md updates"). U-1 through U-5 landed
the durable artifacts (`spec.md`, cycle logs, `compose_*_task` injections,
`feature_memory` MCP tool); this PR wires the worker prompts and the lead
persona to consume them.

After this PR merges, every newly-spawned worker reads:

- **coder.md** — read `## FEATURE SPEC` and `## PREDECESSOR UNITS` BEFORE
  implementing (new workflow step 3). Spec wins on conflict with the unit
  description; PR body must include a `## Spec satisfaction` section
  (satisfied acceptance criteria + declared deviations + predecessor
  alignment); re-read the spec on every fix-loop resume.
- **tester.md** — test against the spec's **Acceptance** criteria, not just
  the unit description. Cross-check predecessor decisions. **Scope
  violations are bugs** — touching code the spec marks out-of-scope emits
  `BUG_FOUND` with a failing test asserting the unrelated code shouldn't
  change.
- **reviewer.md** — new mandatory **Method step 3** (spec-vs-PR-description
  comparison). Method header renumbered "7 → 8 steps"; subsequent step
  numbers (3–7 → 4–8), the "Red Flags" cross-references, and the delta
  re-review's "Run Method steps 4-7" all updated to match. On retry cycles
  the new `## THIS UNIT'S CYCLE LOG` block is read FIRST to avoid re-flagging
  resolved findings. Predecessor inconsistency = 🟠; missing
  `## Spec satisfaction` when spec exists = 🔴.
- **CLAUDE.md** — new `### Feature spec + cycle log discipline (F-006)`
  subsection: call `feature_memory(F-X)` before discussing a feature;
  commit `spec.md` edits with a `Why:` line on non-obvious decisions
  (git log of `spec.md` IS the decision log); cycle logs are read-only
  history (revise `spec.md`, never the cycle log).

## Manual verification needed

None beyond CI — the change is documentation only. The new rules become
operational on the next worker spawn after merge; until then everything
in flight (including this PR's own review) continues to run the
pre-Phase-1 prompts.

## Decisions / deviations from the unit description

- **This PR deliberately does NOT include a `## Spec satisfaction` section
  of its own**, per the IMPORTANT note in U-6's task description and the
  matching decision recorded in `features/F-006/spec.md` § Decisions
  ("U-6 lands LAST and explicitly tells its own coder NOT to apply the new
  rules to its own PR"). Emitting a `## Spec satisfaction` section here
  would be a circular meta-reasoning artifact — the new rules take effect
  only after merge and on the next spawn. Reviewers: treat the prompt-file
  diffs as ordinary markdown review.
- **Added a step to the coder workflow** (new step 3, "Read `## FEATURE SPEC`
  FIRST") rather than folding into the existing "Read the repo" step. The
  proposal explicitly says "Read spec FIRST" — making it a distinct numbered
  step preserves that ordering visibly and keeps the existing `CLAUDE.md` /
  `AGENTS.md` / `CONTRIBUTING.md` reading step intact for project-convention
  loading. Total workflow goes from 11 → 12 steps; the `test_coder_prompt.py`
  sequential-numbering check passes.
- **Inserted reviewer Method step 3 between current 2 and 3 (not at the end)**
  so the spec-compliance check happens after reading conventions but BEFORE
  the deep code review (cross-check, data flow, deletions, tests). The
  spec-vs-PR-description comparison is high-level; running it first lets the
  reviewer apply spec context to the code-review passes that follow.
  Heading text "The Method — 7 steps" → "The Method — 8 steps" updated
  consistently; "Red Flags" step references and "Run Method steps 4-7" in
  the delta section bumped to keep numbers in sync.
- **Removed the bare `**Feature spec** — the broader feature context`
  bullet from reviewer.md's "Your task" section.** It was a vestigial
  placeholder; the actual feature spec content arrives via the new
  `## FEATURE SPEC` context block, which is now documented in detail
  immediately below the bullet list.
- **CLAUDE.md addition placed as `### Feature spec + cycle log discipline
  (F-006)` under "Current capabilities"**, alongside other process /
  discipline subsections (`### Restart recovery flow`, `### Scheduling
  rule`, etc.). Rejected placing it as a top-level `##` section because
  it's about *using* the F-006 tools, not security invariants (those live
  under `## Hard rules`).
- **`docs/PROPOSAL-feature-spec-and-headless-daemon.md` link path** —
  prompts live at `orchestrator/prompts/<role>.md`, so the relative path
  to the proposal is `../../docs/...`. Caught and fixed by
  `test_docs_linkcheck.py`; the test now passes.
- **Did NOT include the Phase 3 "when daemon is running" rule** in
  CLAUDE.md's new section. The unit description scopes CLAUDE.md updates
  to the three F-006 rules (`feature_memory` at session start, `Why:` on
  spec commits, cycle logs read-only). The daemon rule belongs with
  Phase 3 (the daemon feature itself).

## Tests

All 1794 tests still pass. Of particular relevance:

- `tests/test_coder_prompt.py` (8 tests) — workflow step numbering 1..N
  with no gaps, pre-commit self-review preserved, rebase BLOCKED escape
  preserved. ✓
- `tests/test_reviewer_prompt.py` (16 tests) — delta-section invariants
  (`Skip Method step 1`, RESOLVED/NOT_RESOLVED/N/A vocabulary, anti-
  anchoring warnings both directions, fresh terminal marker required,
  all three markers listed). ✓
- `tests/test_docs_linkcheck.py` (35 tests) — all relative links resolve.
  ✓

Generated by orchestrator-coder


---
_Coder session: `sesn_01QASftS2wrEnoikiUTF6XbQ` · unit F-006-U-6_

## Cycle history
3 cycles · cap-3 not hit

### Cycle 0 — coder: PR opened
- PR #50 opened

### Cycle 0 — tester: TESTS_PASS
- All tests pass

### Cycle 1 — coder fix: FIX_PUSHED
- Fix committed and pushed

### Cycle 1 — reviewer: REVIEW_REQUEST_CHANGES
- coder.md resume rule promises orchestrator re-injects `## FEATURE SPEC` on every fix-loop turn but `compose_fix_task` doesn't actually inject it

### Cycle 2 — coder fix: FIX_PUSHED
- Fix committed and pushed

### Cycle 2 — reviewer: REVIEW_RECOMMEND_MERGE
- Endorsed (self-approval blocked): H1/M1/L1 all resolved with code + tests; runtime now matches the prompt's resume-time contract on both coder and reviewer delta paths

### Cycle 3 — coder fix: FIX_PUSHED
- Fix committed and pushed

### Cycle 3 — tester: TESTS_PASS
- All tests pass

### Cycle 3 — reviewer: REVIEW_RECOMMEND_MERGE
- Endorsed (self-approval blocked): H1/M1/L1 all resolved with code + tests; runtime now matches the prompt's resume-time contract on both coder and reviewer delta paths

## Review threads
- orchestrator/prompts/coder.md:231 — ### 🟠 H1 — Resume rule over-claims orchestrator behavior; `## FEATURE SPEC` is NOT re-injected on fix-loop turns The new paragraph promises *"The orchestrator… (https://github.com/boctorj/agent-orchestrator/pull/50#discussion_r3280191261)
- orchestrator/prompts/reviewer.md [outdated] — ### 🟡 M1 — `## THIS UNIT'S CYCLE LOG` block isn't actually present on the normal retry path either *"present only on retry cycles (review_round ≥ 2). Contains… (https://github.com/boctorj/agent-orchestrator/pull/50#discussion_r3280191268)
- tests/test_f006_u6_role_prompt_updates.py [outdated] — ### 🔵 L1 — Off-by-one in comment: pre-PR `coder.md` had 11 steps, not 10 The docstring (line 324) and assertion message (line 330) both claim the old prompt wa… (https://github.com/boctorj/agent-orchestrator/pull/50#discussion_r3280191272)
- orchestrator/prompts/reviewer.md [outdated] — In reviewer.md the paragraph says “read them in this order BEFORE the diff”, but the bullet for `## THIS UNIT'S CYCLE LOG` says “Read this FIRST on retry”. Tho… (https://github.com/boctorj/agent-orchestrator/pull/50#discussion_r3281701033)
- CLAUDE.md [outdated] — Minor formatting: the section reference `§"CLAUDE.md updates"` is missing a space after the section symbol, unlike other references (e.g. `§ "Role prompt chang… (https://github.com/boctorj/agent-orchestrator/pull/50#discussion_r3281701122)
- orchestrator/prompts/coder.md:21 — why is there reference to the proposal spec in the prompt files? (https://github.com/boctorj/agent-orchestrator/pull/50#discussion_r3281710090)
- orchestrator/prompts/coder.md [outdated] — make sure to point this to the target repo and NOT the orchestration repo instance. It may be the same if I am dogfooding but it shouldn't use the currently ru… (https://github.com/boctorj/agent-orchestrator/pull/50#discussion_r3281726662)
- orchestrator/prompts/coder.md:230 — but this surfaces a new issues, so any changes that you approve with the lead after the initial spec approval will be lost if we restart the session. Is that a… (https://github.com/boctorj/agent-orchestrator/pull/50#discussion_r3281778696)
- orchestrator/prompts/tester.md [outdated] — why this reference is here (https://github.com/boctorj/agent-orchestrator/pull/50#discussion_r3281798550)
